rule samtools_index:
    input:
        "sorted_reads/{sample}.sort.bam"
    output:
        "sorted_reads/{sample}.sort.bam.bai"
    log:
        "logs/samtools_index/sorted/{sample}.log"
    shell:
        "samtools index {input} > {log} 2>&1"
